# PLAN13: Фикс багов, улучшение UX и новые фичи декоративного сада

**Проект:** `udachny-sad` (React+TS+Vite+PocketBase+Tailwind)
**Базовый путь:** `/root/projects/udachny-sad/app/`

---

## Этап 1: Исправление критических багов и UX-проблем (BUGS.md)

- [ ] **[CRITICAL]** Рефакторинг Onboarding/Guided-tour.
  - Заменить блокирующий `fixed inset-0 z-50` модал на `spotlight-overlay` (подсветка активного элемента через `box-shadow` или `clip-path`).
  - Добавить обработчик `click-outside` на фоновый слой для автозакрытия тура.
  - Добавить кнопки "Пропустить" и "Далее/Завершить".
- [ ] **[CRITICAL]** Рефакторинг GuidedTour — разделить overlay-слой (который перехватывает pointer events) и tooltip-слой.
  - Overlay должен использовать `pointer-events: none` везде, кроме tooltip-области.
  - Или использовать `spotlight` подход: `box-shadow: 0 0 0 9999px rgba(0,0,0,0.6)` на целевом элементе.
- [ ] **[LOW]** Исправление бага загрузки `telegram-widget.js` (ERR_ABORTED).
  - Реализовать динамическое добавление скрипта через `useEffect` только для production-окружения.
  - Или полностью убрать Telegram login widget если он не используется.
- [ ] **[UX]** Фикс data race при отображении списка садов после логина.
  - Добавить флаг `isAuthReady` и проверять его перед запросом `gardens`.
  - Очищать стейт только при явном логауте, а не при маунте компонента.
  - Добавить `useEffect` dependency на `isAuthReady`.

---

## Этап 2: Улучшение UX (Пустые состояния, переходы, скелетоны)

- [ ] Внедрение UX/UI текстов для Empty States:
  - **Мои сады:** "Ваш сад ждет своих первых жителей" (CTA: Создать первый сад).
  - **Каталог растений:** "Ничего не найдено" (CTA: Сбросить фильтры).
  - **Планировщик/Канвас:** "Полотно чисто" (CTA: Открыть каталог растений).
- [ ] Внедрение Skeleton Loaders:
  - Заменить спиннеры на серые блоки-скелетоны (`animate-pulse bg-gray-200 rounded-md`) для карточек и списков.
  - Скелетон для карточки сада, карточки растения, журнала событий.
- [ ] Плавные визуальные переходы (Tailwind transitions):
  - Реализовать staggered fade-in для карточек с использованием `style={{ animationDelay: \`\${index * 100}ms\` }}` и класса `animate-fade-in-up`.
  - Добавить keyframe `fadeInUp` в `tailwind.config.js`.

---

## Этап 3: Новые фичи — Сезонный превью сада

- [ ] Сезонный превью сада (Timeline Slider):
  - Разработка UI слайдера с выбором месяца (Янв–Дек).
  - Компонент `<SeasonPreview>` с горизонтальным слайдером месяцев.
  - Подсветка/затемнение растений на схеме канваса в зависимости от их сезона цветения.
  - Интеграция с существующими данными о сезонах цветения из PLAN12.
- [ ] Реализация логики подсветки:
  - Растения в цвету → полная яркость + иконка цветка 🌸.
  - Растения вне сезона → пониженная opacity (0.4) + серая отделка.
  - Декоративно-лиственные → всегда видны.

---

## Этап 4: Новые фичи — Экспорт и теги

- [ ] Экспорт плана сада (PDF/PNG):
  - Установить `html2canvas` и `jspdf`.
  - Компонент `<ExportButton>` в канвасе.
  - Генерация отчёта: снимок канваса + список растений + их характеристики.
  - Экспорт в PNG (быстрый снимок) и PDF (список + изображение).
- [ ] Система тегов для растений:
  - Добавление полей в PocketBase schema (`shade`, `sun`, `moisture`, `soil_type`).
  - UI фильтра в каталоге растений: чипы-теги с мульти-выбором.
  - Цветовая кодировка тегов (солнце=жёлтый, тень=серый, влага=синий).
- [ ] Drag-and-drop сортировка растений:
  - Установить `@dnd-kit/core` и `@dnd-kit/sortable`.
  - Включить DnD в карточках растений внутри клумбы/композиции.
  - Сохранение нового порядка в PocketBase.

---

## Этап 5: Уведомления и тесты

- [ ] Уведомления о времени посадки и обрезки:
  - Вывод toast-уведомлений на основе сопоставления текущего месяца с метаданными растений.
  - Раздел "Что сделать сейчас" на главной странице.
  - Иконки: 🌱 посадка, ✂️ обрезка, 💧 полив.
- [ ] Тесты для новых компонентов:
  - Unit-тесты для SeasonPreview, ExportButton, TagFilter.
  - Integration-тест для DnD-сортировки.
  - Проверить что onboarding fix resolved Bug #2 (modal no longer blocks clicks).
