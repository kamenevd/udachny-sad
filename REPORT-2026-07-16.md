# 🌱 уДачный сад — Отчёт для Димы (ночь 16→17 июля)

## Анализ кодовой базы (3 модели: GLM-5.2 + DeepSeek V4 Pro + Nemotron Ultra)

---

## 🔴 Сделать в первую очередь

### 1. Дублирование mutations — ИСПРАВЛЕНО ✅
- `gardensMutations.ts` удалён
- `gardens.ts` расширен: `create` теперь принимает `name + description + width + length`
- Если `width/length` переданы → генерируется `boundary` прямоугольник
- Логика auto-create user (upsert) перенесена из gardensMutations.ts

### 2. Convex deployment — НУЖЕН ТЫ
- `npx convex dev` — интерактивный, требует входа в Convex аккаунт
- Это займёт ~5 минут
- Без этого: нет `VITE_CONVEX_URL`, нет `_generated/` типов, React не подключён к backend
- **Не блокирует дизайн-систему** — это backend, не UI

### 3. localStorage → Convex
- Gardens.tsx работает на localStorage
- После `npx convex dev` нужно обернуть App в ConvexProvider и переписать на hooks
- **Можно делать до дизайн-системы** — data layer не зависит от UI

---

## 🟡 Решения по архитектуре (приняты MoA-тройкой)

### Auth: Email/Password для MVP
- 0 внешних зависимостей, мгновенный запуск
- Google OAuth — позже (нужен Google Cloud Console проект)
- Кнопку Google в UI оставить, скрыть за `VITE_GOOGLE_ENABLED`

### QUESTIONS.md: все 12 рекомендаций приняты
- Q1-Q3 (блокирующие) — ✅ приняты, уже в schema
- Q4 (зоны) — ⚠️ schema.ts УЖЕ реализует вариант B (2 раздельные таблицы). Это ЛУЧШЕ чем рекомендация. QUESTIONS.md обновлён.
- Q5-Q12 — ✅ приняты

---

## 📋 План на утро

1. **Ты:** запускаешь `npx convex dev` в `/root/udachny-sad/app/` → получаешь URL → пишешь в `.env`
2. **Я:** оборачиваю App в ConvexProvider, переписываю Gardens.tsx на hooks
3. **Ты:** передаёшь дизайн-систему от Claude
4. **Я:** применяю дизайн-систему к готовым компонентам

---

## ✅ Текущий статус

| Что | Статус |
|---|---|
| Dev-сервер (udacha.kdnfx.space) | ✅ Работает, systemd |
| TypeScript | ✅ 0 ошибок |
| Schema БД (9 таблиц, 14 индексов) | ✅ Готова |
| Mutations (gardens.ts) | ✅ Исправлено (merge) |
| UI Components (Button, Input, Modal) | ✅ Готовы |
| React-экраны | ✅ Заглушки работают |
| Convex deployment | ❌ Нужен `npx convex dev` |
| Convex Auth | ❌ Email/Password, не подключён |
| Дизайн-система | ⏳ В Claude, ждём |

## ⚠️ PWA иконки
- `icon-192.png` и `icon-512.png` указаны в манифесте, но файлов нет
- Нужно создать (можно из favicon.svg)
