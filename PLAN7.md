# PLAN7 — Миграция на PocketBase + локальная авторизация

> **Цель:** полностью уйти с Convex cloud на локальный PocketBase. Добавить авторизацию через Telegram и Яндекс. Сохранить весь функционал.

## Этап A: PocketBase сервер (infrastructure)

- [ ] Задача A.1: Установить PocketBase на LXC 108 — скачать бинарник v0.23+, создать systemd сервис, запустить на `127.0.0.1:8090`, проверить доступ через `curl http://127.0.0.1:8090/api/health` → файл(ы): `/opt/pocketbase/pocketbase`, `/etc/systemd/system/pocketbase.service` [просто] — **TODO: нет SSH-доступа к LXC 108, скрипт подготовлен в `pocketbase-infra/install.sh`; `curl http://192.168.3.59:8090/api/health` уже отвечает healthy — похоже, инстанс уже поднят кем-то ранее, требуется проверка вручную**
- [ ] Задача A.2: Настроить админ-аккаунт PocketBase — первый запуск `./pocketbase serve` создаёт админа; сменить пароль; проверить вход в админку `http://192.168.3.59:8090/_/` → файл(ы): `/opt/pocketbase/pb_data/` [просто] — **TODO: ручной шаг, креды администратора не должны проходить через агента**
- [x] Задача A.3: Перенести схему Convex → PocketBase migrations — создать migration скрипт `pb_migrations/001_init.js` со всеми таблицами (users, gardens, schemaObjects, lightZones, plants, plantings, journalEvents, photos). Сохранить индексы (`by_owner`, `by_garden`, `by_garden_and_type`) и типы полей. Скрипт должен быть идемпотентным → файл(ы): `pb_migrations/001_init.js` [средне]
- [x] Задача A.4: Настроить правила доступа (API rules) в PocketBase — для каждой коллекции: list/view = `owner = request.auth.id` (или `garden.ownerId = request.auth.id` для связанных), create/update/delete = аналогично. Изолировать данные пользователей → файл(ы): `pb_migrations/002_rules.js` [средне]
- [ ] Задача A.5: Настроить nginx проксирование PocketBase — `pb.kdnfx.space` → `http://192.168.3.59:8090`, HTTPS, CORS заголовки, ограничение размера загрузки фото (`client_max_body_size 20M`) → файл(ы): `/etc/nginx/sites-enabled/kdnfx` [просто] — **TODO: конфиг подготовлен в `pocketbase-infra/nginx-pb.kdnfx.space.conf`, нужен ручной деплой + certbot**

## Этап B: Авторизация (Telegram + Яндекс)

- [ ] Задача B.1: Создать Telegram-бота для Login Widget — через @BotFather получить новый бот `udacha_auth_bot`, установить домен `udacha.kdnfx.space` в настройках бота, записать token → файл(ы): `.env.local` [просто] — **TODO: ручное действие (BotFather), не автоматизируется**
- [ ] Задача B.2: Получить Яндекс OAuth Client ID — зарегистрировать приложение на `https://oauth.yandex.ru`, redirect URI = `https://udacha.kdnfx.space/auth/yandex/callback`, scopes: `login:birthday login:email login:info login:avatar`, записать Client ID и Client Secret → файл(ы): `.env.local` [просто] — **TODO: ручное действие (регистрация в Yandex OAuth), не автоматизируется**
- [ ] Задача B.3: Настроить OAuth провайдеры в PocketBase — в админке Settings → Auth providers: включить Yandex (вставить client id + secret) → файл(ы): `pb_data/` [просто] — **TODO: ручной шаг через админку + Telegram НЕ является OAuth2-провайдером PocketBase (нет такого пункта в Settings → Auth providers), реализован отдельным custom-роутом `pb_hooks/telegram_auth.pb.js`, см. README-PocketBase.md**
- [x] Задача B.4 (частично): `app/src/lib/auth.ts` реализован (loginWithTelegram через `mountTelegramLoginWidget` + `pb_hooks/telegram_auth.pb.js`, loginWithYandex, logout, currentUser). `Login.tsx` **пока не переключён** — main.tsx всё ещё роутит по Convex `Authenticated`/`Unauthenticated`, переключать раньше времени = сломать единственный рабочий вход в приложение без готовой замены (OAuth-креды не получены, экраны C.2-C.7 ещё на Convex). Переключение будет одним коммитом вместе с D.7 (провайдер в main.tsx) и завершением C.
- [ ] Задача B.5: Удалить зависимости Convex Auth — убрать `@convex-dev/auth`, `convex/auth.ts`, `convex/auth.config.ts`. Очистить schema от authTables → файл(ы): `package.json`, `app/convex/auth.ts`, `app/convex/auth.config.ts` [просто] — **не раньше, чем экраны (C) перестанут зависеть от ConvexAuthProvider**

## Этап C: API слой — адаптация frontend

- [x] Задача C.1: Создать API client на PocketBase SDK — `app/src/lib/pb.ts`: инициализация `PocketBase` инстанса, типизированные обёртки для CRUD: `gardens.list()`, `gardens.create()`, `gardens.update()`, `gardens.delete()`. Аналогично для plantings, events, photos → файл(ы): `app/src/lib/pb.ts` [средне]
- [x] Задача C.2: Мигрировать Gardens screen с Convex на PocketBase — заменить `useQuery(api.gardens.list)` на PocketBase SDK вызовы через хуки `useEffect + useState`. Сохранить loading/error состояния. Оптимистичные обновления (optimistic UI) через `useSafeMutation` паттерн → файл(ы): `app/src/screens/Gardens.tsx` [средне] — сделано: `lib/pbStats.ts`/`lib/pbBackup.ts` (клиентские аналоги convex/stats.ts, convex/backup.ts, convex/gardens.ts remove-каскада), `hooks/useSafePbAction.ts`. `main.tsx` кастует gardenId в `Id<"gardens">` на переходе к GardenDetail (ещё не мигрирован) — уйдёт вместе с C.3. Не проверено на живом PocketBase (нет доступа к серверу).
- [x] Задача C.3: Мигрировать GardenDetail (canvas) — заменить Convex queries для schemaObjects и lightZones на PocketBase. Обновить реальный-тайм подписки: Convex `onUpdate` → PocketBase `collection.subscribe`. Canvas должен обновляться в реальном времени → файл(ы): `app/src/screens/GardenDetail.tsx` [сложно] — сделано вместе с C.4/C.5 одним коммитом (см. ниже), `hooks/usePbCollection.ts` — realtime-список через `subscribe("*")` + refetch.
- [x] Задача C.4: Мигрировать Plants + PlantingDetail — заменить Convex queries/mutations для plants и plantings. Реализовать list/detail через PocketBase. Сохранить функции undo/redo → файл(ы): `app/src/screens/Plants.tsx`, `app/src/screens/PlantingDetail.tsx` [средне] — сделано, включая `PlantingForm.tsx`, `StatusActions.tsx`, `EventForm.tsx` (делят общие компоненты с GardenDetail/PlaceHistory — мигрированы одним коммитом, иначе нерабочее промежуточное состояние). `lib/pbPlantings.ts` — авто-события журнала (create/close/transplant).
- [x] Задача C.5: Мигрировать PlaceHistory + Events — заменить Convex queries/mutations для journalEvents. Timeline через PocketBase SDK → файл(ы): `app/src/screens/PlaceHistory.tsx` [средне] — сделано вместе с C.3/C.4 (`PlaceHistoryPrint.tsx`, `SeasonReport.tsx` тоже). PhotoGallery/PhotoUpload/AiAdvice/AiCareTip/PhotoDiagnosis сознательно оставлены на Convex (C.6/C.7) — мост через `as unknown as Id<...>` в местах вызова. Не проверено на живом PocketBase.
- [x] Задача C.6: Мигрировать Photos — переключить хранение фото на PocketBase file storage (одно из главных преимуществ!). Заменить `generateUploadUrl` + `store` Convex паттерн на `formData.append()` → `pb.collection('photos').create(formData)`. Получать фото через `pb.files.getUrl()` → файл(ы): `app/src/components/PhotoUpload.tsx`, `app/src/components/PhotoGallery.tsx` [средне] — сделано: PhotoUpload шлёт FormData одним запросом (`photos.create`), PhotoGallery — `usePbCollection` (realtime) + `photoUrl()`. Заодно закрыт TODO про orphan-фото: `deletePhotosFor()` (lib/pbBackup.ts, экспортирован) теперь вызывается перед удалением owner-записи не только в каскаде участка, но и в единичных местах (ObjectSheet — списание объекта схемы, PlantingDetail — удаление записи журнала). PhotoDiagnosis (AI-функция) остаётся на Convex — задача C.7. Не проверено на живом PocketBase (нет доступа к серверу).
- [ ] Задача C.7: Мигрировать AI-функции (AiAdvice, PhotoDiagnosis) — PocketBase не имеет serverless functions. Реализовать GLM прокси: endpoint `POST /api/ai/advice` который аутентифицирует запрос, вызывает GLM, возвращает ответ. Использовать Fastify middleware (PocketBase расширяется через Go, но для JS-прокси проще отдельный Fastify) → файл(ы): `server/ai-proxy.ts` [сложно]

## Этап D: Очистка и финализация

- [ ] Задача D.1: Удалить весь каталог `app/convex/` — все 44 API функции больше не нужны → файл(ы): `app/convex/` [просто]
- [ ] Задача D.2: Убрать Convex из зависимостей — удалить `convex`, `@convex-dev/auth`, `@convex-dev/serverless`. Обновить `package.json`, выполнить `npm install` → файл(ы): `package.json` [просто]
- [ ] Задача D.7: Убрать ConvexProvider из `main.tsx` — заменить на `PocketBaseProvider` (или просто инициализацию клиента в `lib/pb.ts`). Убрать `ConvexProviderWithCliAuth` → файл(ы): `app/src/main.tsx` [просто]
- [ ] Задача D.3: Настроить бэкап PocketBase — cron-задача: ежедневный `cp pb_data/backup/$(date +%F).db`, хранить последние 30 дней. Дополнительно: rsync на NAS LXC 103 для офсайт-копии → файл(ы): `/opt/pocketbase/backup.sh`, `/etc/cron.daily/pocketbase-backup` [просто]
- [ ] Задача D.4: Написать README-PocketBase.md — документация по запуску: установка, миграции, OAuth настройка, бэкап, troubleshooting → файл(ы): `README-PocketBase.md` [просто]
- [ ] Задача D.5: Обновить README.md — убрать упоминания Convex, добавить PocketBase. Обновить секцию «Architecture» → файл(ы): `README.md` [просто]
- [ ] Задача D.6: Финальный smoke-тест — полный flow: вход через Telegram → создание сада → рисование схемы → посадки → события → фото. Зафиксировать в `REPORT-MIGRATION.md` → файл(ы): `REPORT-MIGRATION.md` [просто]

## Этап E: Дополнительно (после основной миграции)

- [ ] Задача E.1: Реализовать опрос Yandex Dictionary API для автозаполнения растений — поиск растения по названию → предзаполнение данных (высота, период цветения) → файл(ы): `app/src/lib/yandex-dict.ts` [средне]
- [ ] Задача E.2: Добавить seed-данные для демо — скрипт для заполнения PocketBase тестовыми садами и растениями для демонстрации → файл(ы): `pb_migrations/003_seed.js` [просто]
- [ ] Задача E.3: Реализовать PWA offline-first кэширование PocketBase данных — стратегия stale-while-revalidate для садов/растений, кэширование фото в Cache Storage API → файл(ы): `app/src/lib/offline-cache.ts`, `app/src/hooks/useCachedPBQuery.ts` [сложно]
- [ ] Задача E.4: Реализовать ежесезонный бэкап экспорта — кнопка «Экспортировать сезон» в PlaceHistory: выгрузка всех данных за год в JSON + ZIP с фото → файл(ы): `app/src/utils/exportSeason.ts` [средне]
- [ ] Задача E.5: Настроить Lighthouse audit — запустить build, проверить scores. Зафиксировать метрики → файл(ы): `REPORT-FINAL.md` [просто]
- [ ] Задача E.6: Написать unit-тесты для API слоя PocketBase — мокирование PocketBase SDK, тесты на CRUD операции → файл(ы): `app/src/__tests__/pb-api.test.ts` [средне]
