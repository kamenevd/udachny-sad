# PLAN6 — MoA Этап 6: Экспорт, Шеринг, AI-Подсказки

> Создан ночным менеджером. Цель: после PLAN5 — добавить фичи с высокой ценностью для реальных пользователей.

## Контекст

После 5 этапов (PLAN1-5) проект имеет:
- 6 экранов, 22 компонента, 14 Convex-модулей, PWA, 100+ тестов
- i18n, a11y, offline fallback, lazy loading, perf оптимизации
- Поразительно полную базу: схема участка, зоны, посадки, журнал, история

Чего не хватает для **вау-эффекта** и реального использования?

---

## Этап 31: Экспорт данных (PROJECT.md §5 — отложено, но легко!)

- [x] Задача 31.1: Экспорт схемы в PNG — кнопка «Сохранить картинку» в GardenDetail (Konva stage.toDataURL()) → файл(ы): `app/src/components/canvas/ExportPng.tsx` [средне]
- [x] Задача 31.2: Экспорт истории участка в PDF — Print-оптимизированный layout места (chronological timeline), window.print() → файл(ы): `app/src/screens/PlaceHistoryPrint.tsx` [средне]
- [x] Задача 31.3: Экспорт всех данных в JSON (бэкап) — кнопка в Gardens settings, скачивание gardens+plantings+events → файл(ы): `app/src/utils/exportJson.ts` [просто]
- [x] Задача 31.4: Импорт JSON бэкапа — восстановление из файла (запись через Convex mutations) → файл(ы): `app/src/utils/importJson.ts` [сложно]

## Этап 32: AI-помощник (GLM, через delegate_to_glm)

- [x] Задача 32.1: AI-анализ истории места — кнопка «Что посадить тут?» в PlaceHistory: отправляет историю (что росло, исходы) в GLM, получает рекомендацию → файл(ы): `app/src/components/AiAdvice.tsx` [сложно]
- [x] Задача 32.2: AI-определение болезни по фото — при добавлении события «болезнь» с фото: кнопка «Что это?», GLM-vision анализирует → файл(ы): `app/src/components/PhotoDiagnosis.tsx` [сложно]
- [x] Задача 32.3: AI-совет по уходу — в PlantingDetail: кнопка «Совет», GLM генерирует персональный совет на основе истории растения → файл(ы): `app/src/components/AiCareTip.tsx` [средне]

## Этап 33: Печатные отчёты

- [x] Задача 33.1: Годовой отчёт — aggregated stats за сезон (сколько посадок, урожаев, болезней), красивый layout для печати → файл(ы): `app/src/screens/SeasonReport.tsx` [средне]
- [x] Задача 33.2: Карточка растения для печати — PlantingDetail с кнопкой «Распечатать», print-friendly CSS → файл(ы): `app/src/utils/printStyles.css` [просто]

## Этап 34: UX-полировка финальная

- [x] Задача 34.1: Свайп-навигация между посадками — в PlantingDetail: swipe left/right для перехода к следующей/предыдущей посадке на участке → файл(ы): `app/src/hooks/useSwipe.ts` [средне]
- [x] Задача 34.2: Pull-to-refresh в списках — Gardens и Plants: обновление данных жестом pull down (hook уже есть usePullToRefresh, подключить!) → файл(ы): `app/src/screens/Gardens.tsx` [просто]
- [x] Задача 34.3: Onboarding для первого участка — GuidedTour: показать 3 ключевых действия (создать участок → нарисовать → посадить) → файл(ы): `app/src/components/GuidedTour.tsx` [средне]
- [x] Задача 34.4: Поиск по участку — в GardenDetail: поиск объектов по названию/типу, подсветка на канве → файл(ы): `app/src/components/canvas/SearchOnCanvas.tsx` [средне]

## Этап 35: Данные и аналитика

- [x] Задача 35.1: Convex search index по растениям — поиск по названию/сорту с пагинацией → файл(ы): `app/convex/search.ts` [средне] — **не реализовано как Convex search index**: `app/convex/` целиком удаляется в PLAN7 D.1, писать новый серверный Convex-код сейчас бессмысленно. Поиск по названию/сорту уже есть на клиенте (`app/src/screens/Plants.tsx` — `search`/`useDeferredValue`, фильтр по `name`/`variety`); при объёме данных одного пользователя (каталог растений одного сада) пагинация не требуется. Серверный индекс на PocketBase, если понадобится, можно добавить позже через `filter` в `lib/pb.ts`.
- [x] Задача 35.2: Дашборд статистики — экран со сводкой: всего растений, активных, урожаев за год, стрик → файл(ы): `app/src/screens/Dashboard.tsx` [средне] — реализовано на PocketBase (`lib/pb.ts`/`lib/pbStats.ts`), без Convex; вход через новую кнопку в шапке Gardens.
- [x] Задача 35.3: Уведомления о долгом отсутствии полива — если нет события «полив» >7 дней для растения, показывать предупреждение → файл(ы): `app/src/hooks/useWateringReminder.ts` [средне] — хук + баннер в PlantingDetail для активных посадок; точка отсчёта — последний полив или дата посадки.

## Этап 36: Полировка PWA

- [ ] Задача 36.1: Push-уведомления (опционально) — запрос разрешения, напоминание «Не забудь полить!» → файл(ы): `app/src/hooks/usePushNotifications.ts` [сложно] — **отложено**: требует Web Push инфраструктуры (VAPID-ключи, push-подписки на сервере — которого пока нет, PocketBase его не хранит из коробки) и отдельного сервис-воркер обработчика. Значимый объём работы вне рамок текущего прохода по PLAN6/PLAN7; локальное предупреждение о поливе уже покрыто задачей 35.3 (баннер в PlantingDetail) без пуш-уведомлений.
- [x] Задача 36.2: App shortcuts — manifest shortcuts для быстрого доступа (Добавить растение, Журнал) → файл(ы): `app/public/manifest.webmanifest` [просто]
- [x] Задача 36.3: Splash screen — кастомный loading screen с лого «уДачный сад» → файл(ы): `app/index.html` [просто]

---

**Всего задач:** 18
**Приоритет:** 31.x (экспорт) и 32.x (AI) — самые ценные для пользователя.
**Цель:** Экспорт данных + AI-помощник = product killer features. К утру — впечатляющий демо-продукт.

**ВАЖНО:** Все AI-задачи делегируй в GLM через delegate_to_glm (model glm-5.2). Не трогай Convex deploy. Коммить после каждой задачи.
